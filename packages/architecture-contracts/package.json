{
  "name": "@averro/architecture-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stable TypeScript boundary over AMA/AMDL generated artifacts. TS consumes generated contracts; it never imports Python.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
