import { ComingSoon } from "@/components/layout/coming-soon";

export default function CriadorDeReelsPage() {
  return (
    <ComingSoon
      title="Criador de Reels"
      description="Fluxo em etapas: avatar, voz e roteiro, com job de renderização em fila."
      phase={5}
    />
  );
}
